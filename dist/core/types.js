// src/core/types.ts — Shared domain types
export const DEFAULT_SETTINGS = {
    authToken: null,
    authExpiry: null,
    viewer: null,
    enabledSources: ['mangadex'],
    syncOnRead: false,
    autoCreateEntry: true,
};
